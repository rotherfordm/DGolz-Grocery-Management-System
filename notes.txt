create table Transactions
(
PrimaryKey
TransactionID
Date
ProductID
Quantity
)

SELECT TransactionID, SUM(Product) WHERE TransactionID = Current TransactionID)
(
SELECT TransactionID, (Products.Price * Transactions.Quantity) AS 'Product'
FROM Transactions INNER JOIN Products
ON Transactions.ProductID = Products.ProductID
)

